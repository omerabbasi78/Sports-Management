@model IEnumerable<PagePermissions>

@{
    ViewBag.Title = "PagePermissions";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>PagePermissions</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PermissionId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MenuItemId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PermissionId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MenuItemId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PagePermissionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PagePermissionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PagePermissionId })
        </td>
    </tr>
}

</table>
